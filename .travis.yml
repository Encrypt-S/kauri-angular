language: node_js

sudo: required

node_js: stable

os:
  - linux

before_install:
  - npm install -g --silent @angular/cli

addons:
- chrome: stable

install:
  - npm install

script:
  - ng test
  - ng build --prod

cache:
  directories: node_modules

branches:
  only:
  - master

notifications:
  email:
    on_failure: change
    on_success: change
